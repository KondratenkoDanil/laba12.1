#include <iostream>
#include <cmath>

int main() {
    // Оголошення змінних
    double Y;         // Результат
    const double A = 25.0;  // Кут 25 градусів

    // Асемблерна вставка
    __asm {
        fld1                // Завантаження 1 у стек
        fldpi               // Завантаження числа Pi у стек
        fld A              // Завантаження кута A (25 градусів)
        fdiv st(0), st(1)   // Перетворення градусів у радіани
        fptan               // Обчислення tan(A)
        fmul st(0), st(1)   // Множення tan(A) * 3 (3 * tan(25°))
        fld A               // Завантаження кута A (25 градусів)
        fdiv st(0), st(1)   // Перетворення градусів у радіани
        fsin                // Обчислення sin(A)
        fmul st(0), st(1)   // Множення результатів (3 * tan(25°) * sin(25°))
        fld1                // Завантаження 1 у стек
        fld st(1)           // Копія поточного значення
        fadd                // Обчислення sin * tg
        fld1                // Завантаження 1 у стек для arcsin
        fmul st(0), st(1)   // Множення на 5
        fadd                // Арксинус (arcsin)
        fmul st(0), st(1)   // Множення на 5
        fstp Y              // Збереження результату в змінну Y
    }

    // Виведення результату
    std::cout << "Y = " << Y << std::endl;

    return 0;
}
